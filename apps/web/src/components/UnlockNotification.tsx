// =============================================================================
// BATTLECRAPS — UNLOCK NOTIFICATION (STUBBED)
// apps/web/src/components/UnlockNotification.tsx
//
// Replaced by the cinematic unlock modal (FB-012). This component is retained
// as a stub so existing import sites compile cleanly during the transition.
// =============================================================================

import React from 'react';

export const UnlockNotification: React.FC = () => null;
