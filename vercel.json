{
  "buildCommand": "git fetch --unshallow || true && npm run build -w @battlecraps/shared && npm run build -w @battlecraps/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
